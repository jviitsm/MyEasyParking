<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.whatsappandroid.cursoandroid.myeasyparking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.TelaSplash" >

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.TelaCadastro"></activity>
        <activity android:name=".Activities.TelaGerenciar"></activity>
        <activity android:name=".Activities.TelaCadastrarVaga"></activity>
        <activity android:name=".Activities.TelaLogin"></activity>
    </application>

</manifest>